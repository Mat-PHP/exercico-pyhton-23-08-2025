print(input("cadastrar_usuario"))
print(input("cadastrar senha "))
print(input("sair(sim) (nao)"))

def cadastrar_usuario(usuarios, senha):
    if usuarios in usuarios:
        return "usuarios ja existe !"
        usuarios[usuarios]= senha
        return "usuario cadastrado com sucesso"

        def login(usuario,senha):
            global usuario_logado
            if usuario in usuario and usuario[usuario] == senha:
                usuario_logado=usuario
                return f"login realizado com sucesso! seja bem vindo"
            return f"usario incorreto ou senha incorreta"








        def senha (senha , logado):
            if senha  in senha  and senha [senha ] == senha:
                usuarios_logado = senha
                return f"senha realizado com sucesso! seja bem vindo"
            return "usuario incorreto ou senha incorreta"


print("usuario e senha realizado com suceso")
